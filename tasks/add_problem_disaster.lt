{ binding, }{{
  some( id(node, beginning) ).
  some( id(node, disaster) ).
  some( id(node, problem_state) ).

  some( id(chr, protagonist) ).
  some(
    id(chr, protagonist),
    has_property,
    at(id(node, beginning)),
    prop(comfortable_life)
  ).
}}
