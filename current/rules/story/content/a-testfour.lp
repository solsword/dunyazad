% This file defines four fake actions to test a crash.

action(one).
argument(one, first, actor).

action(two).
argument(two, first, actor).

action(three).
argument(three, first, actor).

action(four).
argument(four, first, actor).

